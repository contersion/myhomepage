version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-homepage
    restart: unless-stopped
    ports:
      - "${PORT:-3456}:3000"
    environment:
      # 基础配置
      - NODE_ENV=production
      - PORT=3000
      
      # 数据库配置
      - DATABASE_URL=${DATABASE_URL:-file:./storage/data/app.db}
      
      # Session Secrets（生产环境必须修改）
      - VISITOR_SESSION_SECRET=${VISITOR_SESSION_SECRET}
      - ADMIN_SESSION_SECRET=${ADMIN_SESSION_SECRET}
      
      # 初始密码（首次启动后请立即修改）
      - VISITOR_PASSWORD=${VISITOR_PASSWORD:-visitor123}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      
      # 上传配置
      - UPLOAD_DIR=${UPLOAD_DIR:-./storage/uploads}
      - MAX_UPLOAD_SIZE=${MAX_UPLOAD_SIZE:-5242880}
    volumes:
      # SQLite 数据库持久化
      - ./storage/data:/app/storage/data
      # 上传文件持久化
      - ./storage/uploads:/app/storage/uploads
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => r.statusCode === 200 ? process.exit(0) : process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
